import { Section } from "@/components/ui/section";
import { SectionHeading } from "@/components/sections/section-heading";

export function Work() {
  return (
    <Section id="work" container="default" spacing="default">
      <SectionHeading
        eyebrow="Work"
        title="Work Section"
        description="Placeholder content for the work section."
      />
    </Section>
  );
}
