# Asset conventions

Keep source-controlled, importable visual assets here. Put public, URL-addressable
files in `public/`. Optimized content imagery should be added only when a portfolio
experience needs it.
